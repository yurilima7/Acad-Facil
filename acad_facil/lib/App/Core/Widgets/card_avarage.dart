import 'package:flutter/material.dart';

class CardAvarage extends StatelessWidget {
  const CardAvarage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Card(

    );
  }
}
